# Image asset structure

All project images should live under `/public/images` and be referenced with absolute paths.

## Active image

- `/public/images/hero/hero-bg.svg`

## Naming convention

Use **kebab-case** for all image file names (example: `hero-bg.svg`).
